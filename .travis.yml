language: php

php:
   - 5.2
   - 5.3
   - 5.4

install:
   - mysql --version
   - chmod +x tests/setup-travis.sh
   - ./tests/setup-travis.sh

script:
   - phpunit -c tests/full.xml

notifications:
   email:
      recipients:
         - travis-ci-monitor@sallycms.de
